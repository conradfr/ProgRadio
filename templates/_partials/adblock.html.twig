{% if app.request.locale != 'fr' or (no_affil is defined and no_affil == true) %}
    {% include('_partials/adsense_tag.html.twig') %}
{% else %}
    {{ oneAffiliateLink(app.request.locale)|raw }}
{% endif %}
